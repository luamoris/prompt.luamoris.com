// Import
import DataInteger from "../../classes/data/DataInteger.js";
import seed from "./option.js"

// Seed

class DataSeed extends DataInteger {

   constructor() {
      super(seed);
   }

   // ===

   // get(text, versionId) {}

   // ===

   // pattern() { return new RegExp(); }

   // toParam(title, argument, isNull = true) {}

   // toString(argument) {}

   // findInText(text) {}

   // removeInText(text) {}

   // getSuppurtOpt(versionId) {}

   // isArgument(argument, support) {}
}

export default DataSeed;