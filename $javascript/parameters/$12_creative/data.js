import DataOnly from "../../classes/data/DataOnly.js";
import creative from "./option.js"

// Creative

class DataCreative extends DataOnly {

   constructor() {
      super(creative);
   }

   // ===

   // get(text, versionId) {}

   // ===

   // pattern() { return new RegExp(); }

   // toParam(title, argument, isNull = true) {}

   // toString(argument) {}

   // findInText(text) {}

   // removeInText(text) {}

   // getSuppurtOpt(versionId) {}

   // isArgument(argument, support) {}
}

export default DataCreative;