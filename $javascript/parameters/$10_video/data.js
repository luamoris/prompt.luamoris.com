import DataOnly from "../../classes/data/DataOnly.js";
import video from "./option.js"

// Video

class DataVideo extends DataOnly {

   constructor() {
      super(video);
   }

   // ===

   // get(text, versionId) {}

   // ===

   // pattern() { return new RegExp(); }

   // toParam(title, argument, isNull = true) {}

   // toString(argument) {}

   // findInText(text) {}

   // removeInText(text) {}

   // getSuppurtOpt(versionId) {}

   // isArgument(argument, support) {}
}

export default DataVideo;