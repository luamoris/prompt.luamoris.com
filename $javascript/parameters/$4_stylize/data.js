// Import
import DataBetween from "../../classes/data/DataBetween.js";
import stylize from "./option.js"

// Stylize

class DataStylize extends DataBetween {

   constructor() {
      super(stylize);
   }

   // ===

   // get(text, versionId) {}

   // ===

   // pattern() { return new RegExp(); }

   // toParam(title, argument, isNull = true) {}

   // toString(argument) {}

   // findInText(text) {}

   // removeInText(text) {}

   // getSuppurtOpt(versionId) {}

   // isArgument(argument, support) {}
}

export default DataStylize;